import { dots, line } from "../main.js";
import drawLine from "../render/drawLine.js";
import drawDot from "../render/drawDot.js";

export default function onClick(event) {
    /*drawDot(event);

    if (dots.children.length == 8) {
        drawLine();
    }
    if (dots.children.length > 8) {
        dots.removeChildren();
        line.clear();
    }*/
}
